# Vocabahn — Backlog

- [x] 1. Create complete documentation and 1-step setup for deployment on the server  ✓ DEPLOYMENT.md + scripts/deploy.sh + docker-compose.production.yml + nginx.production.conf
- [x] 2. Suggest a very good architecture for having the data in a safe place with multilayer backup system  ✓ BACKUP.md + scripts/backup.sh + scripts/restore.sh (3-layer: pre-deploy/daily cron/S3 offsite)
- [x] 3. Add versioning (user-visible version, CHANGELOG autogenerated from commits, git version tags)  ✓ CHANGELOG.md, scripts/release.sh, v1.0.0 in footer linking to CHANGELOG
- [x] 4. Create some good quality decks so the user can easily start working with the app  ✓ scripts/seed-decks.ts — 6 themed courses (A1 Basics, A1 Greetings, A1 Numbers/Time, A2 Travel, A2 Food, B1 Work/Daily Life) + pnpm seed:decks
- [x] 5. Write Playwright e2e tests  ✓ playwright.config.ts + e2e/landing.spec.ts + e2e/dictionary.spec.ts + e2e/review.spec.ts (mocked API, keyboard flow, a11y checks)
- [x] 6. Provide a way for the user to create custom decks (public and private, shown separately)  ✓ UserDeck+UserDeckWord Prisma models, full CRUD API (/api/v1/decks), DecksPage + DeckDetailPage, "Add to deck" selector on dictionary entries, shown separately (My decks / Public decks)
- [x] 7. User should be able to flag already-known words (bulk select option)  ✓ markWordKnown endpoint + "Mark as known" on DictionaryCard + bulk select/undo on KnownWordsPage
- [x] 8. User should see the daily enrichment limit  ✓ /api/v1/dictionary/quota endpoint + progress bar in ProfilePage
- [x] 9. Design a landing page describing the app's purpose  ✓ LandingPage component in App.tsx (hero, features grid, sign-in CTA)
- [x] 10. Design some graphics for the app  ✓ Illustrations.tsx — 5 inline SVG components (Dictionary, Flashcard, Trophy, Empty, Streak) used in landing hero, feature cards, and KnownWordsPage empty state
- [x] 11. Provide more animation, more gestures  ✓ review card-back slide-in on reveal, session summary pop+stagger entrance, deck modal slide-up (all with prefersReducedMotion guard)
- [x] 12. Offline mode should work better; option to download a smaller dataset  ✓ GET /api/v1/dictionary/offline-pack returns top-1000 enriched words as JSON download; "Download offline pack" button in ProfilePage
- [x] 13. In case of a report, a better model should evaluate and re-enrich the word  ✓ DOWN vote + content issues → requestReenrichment() → BullMQ job with betterModel:true → gemini-2.0-flash instead of gemini-flash-lite-latest
- [x] 14. Dark/light theme  ✓ done in Phase 5
- [x] 15. Other sign-in options (email / Apple)  ✓ Email magic-link auth: POST /auth/email/request + GET /auth/email/verify, nodemailer sender, EmailOtp Prisma model, SignInOptions component in ProfilePage (Apple requires Apple Developer account — not implemented)
