the UI is too simple, add some animation to the logo in the header.
the vocahban is actually voca + bahn, make the branding in a way that it shows the connection
use GSAP everywhere possible, think of it as a tool that we need to use alongside the learning process.
the sign in flow should be more realistic, doesnt seem like a real sign in. 
implement one click google login
